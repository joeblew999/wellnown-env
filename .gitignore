# Reference source clones (keep Taskfile and docs)
.src/*
!.src/Taskfile.yml
!.src/README.md
!.src/.gitignore

# Playwright screenshots
.playwright-mcp/

# Go binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Command binaries (Go builds binary with directory name inside that directory)
# These match the exact binary files, not source directories
cmd/nats-node/nats-node
cmd/pc-node/pc-node
cmd/wellknown-check/wellknown-check

# Example binaries
examples/narun-hello/narun-hello
examples/conf-only/conf-only
examples/vals-only/vals-only

# Runtime data and logs (volume mounts in production)
# .data/ - MUST persist (JetStream state, KV stores)
# .logs/ - SHOULD persist (debugging, auditing)
# .auth/ - Auth credentials (tokens, nkeys, creds)
**/.data/
**/.logs/
**/.auth/

# Legacy paths (remove after cleanup)
**/nats-node/data/
**/nats-node/logs/
process-compose.log
**/process-compose.log

# Test binaries
*.test

# Output of go coverage
*.out

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
